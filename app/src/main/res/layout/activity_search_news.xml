<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".SearchNewsActivity"
    android:background="#E0FFFF">

    <TextView
        android:id="@+id/search_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ค้นหาข่าว"
        android:textSize="24sp"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp" />

    <EditText
        android:id="@+id/search_by_name_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="กรอกชื่อหรือหัวข้อข่าว"
        android:layout_below="@id/search_title"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/search_by_name_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ค้นหา"
        android:layout_below="@id/search_by_name_edit_text"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="32dp" />

    <EditText
        android:id="@+id/search_by_date_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="เลือกวันที่"
        android:focusable="false"
        android:clickable="true"
        android:layout_below="@id/search_by_name_button"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/search_by_date_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ค้นหา"
        android:layout_below="@id/search_by_date_edit_text"
        android:layout_alignParentEnd="true" />

    <Spinner
        android:id="@+id/time_period_spinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_by_date_button"
        android:layout_marginTop="16dp" />


    <!-- Bottom Navigation -->
<com.google.android.material.bottomnavigation.BottomNavigationView
android:id="@+id/bottom_navigation"
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:layout_alignParentBottom="true"
android:background="#E0FFFF"
app:menu="@menu/bottom_navigation_menu"/>


</RelativeLayout>


